{
  "name": "pulse-ai-root",
  "private": true,
  "version": "1.0.0",
  "description": "Root configuration for PulseAI news hub",
  "scripts": {
    "scraper": "cd automation && node index.js",
    "frontend:dev": "cd frontend && npm run dev",
    "frontend:build": "cd frontend && npm run build",
    "deploy": "node automation/prerender.js && npm run frontend:build && npx firebase deploy --project news-b5c94",
    "deploy:hosting": "node automation/prerender.js && npm run frontend:build && npx firebase deploy --only hosting --project news-b5c94"
  },
  "devDependencies": {
    "firebase-tools": "^13.12.0"
  }
}
